{
  "name": "@strophic/email",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Provider-abstracted transactional email (Resend at launch). Workers-safe (HTTP only).",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "eslint": "10.6.0",
    "typescript": "6.0.3"
  }
}
